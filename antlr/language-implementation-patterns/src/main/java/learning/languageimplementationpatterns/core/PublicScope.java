package learning.languageimplementationpatterns.core;

import br.com.recatalog.util.PropertyList;

public class PublicScope extends BaseScope {

	public PublicScope(PropertyList properties) {
		super(properties);
	}
}