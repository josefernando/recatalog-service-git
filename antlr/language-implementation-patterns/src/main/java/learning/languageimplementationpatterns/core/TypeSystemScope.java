package learning.languageimplementationpatterns.core;

import br.com.recatalog.util.PropertyList;

public class TypeSystemScope extends BaseScope {
	public TypeSystemScope(PropertyList properties) {
		super(properties);
	}
}